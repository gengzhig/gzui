{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "chrome",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}",
      "program": "${file}"
    },
    {
      "type": "pwa-msedge",
      "request": "launch",
      "name": "edge",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}",
      "program": "${file}"
    }
  ]
}